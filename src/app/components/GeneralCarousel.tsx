import React from "react";
import { UncontrolledCarousel } from "reactstrap";

const GeneralCarousel = ({items}: any) => <UncontrolledCarousel items={items} />;

export default GeneralCarousel;
